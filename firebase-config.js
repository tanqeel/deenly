/* ============================================================
   FILL THIS IN with your own Firebase project's web config,
   then flip CLOUD_ENABLED to true. Steps are in SETUP.md.
   This file is safe to be public — Firebase web config is not
   a secret; access is controlled by the security rules you'll
   paste into the Firebase console (also in SETUP.md).
   ============================================================ */
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};

// Leave this false until you've filled in the real values above —
// the app works perfectly well offline-only with this off.
export const CLOUD_ENABLED = false;
